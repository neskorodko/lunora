/**
 * Модуль для режиму гадання по руці (хіромантія)
 * @module modes/palmreading
 */

const { Markup } = require('telegraf');
const fs = require('fs');
const path = require('path');
const { deductCoins, logActivity } = require('../helpers/utils');
const { callOpenAI } = require('../helpers/openai');

/**
 * Налаштування режиму гадання по руці для бота
 * @param {Telegraf} bot - Екземпляр Telegraf бота
 */
function setupPalmReadingMode(bot) {
  // Меню режиму гадання по руці
  const palmReadingMenuKeyboard = Markup.keyboard([
    ['👋 Загальний аналіз руки', '❤️ Лінії кохання'],
    ['💼 Кар\'єра і достаток', '🧬 Здоров\'я і довголіття'],
    ['🔄 Повернутися в головне меню']
  ]).resize();

  // Обробка вибору режиму гадання по руці
  bot.hears('✋ Гадання по руці', async (ctx) => {
    ctx.session.mode = 'palmreading';
    await ctx.reply(`Ви вибрали режим "Гадання по руці".
    
Завантажте чітке фото своєї долоні для аналізу. Найкраще працює фото внутрішньої сторони лівої руки при гарному освітленні.

Оберіть тип аналізу:`, palmReadingMenuKeyboard);
    logActivity(ctx, 'Відкрив режим гадання по руці');
  });

  // Повернення в головне меню
  bot.hears('🔄 Повернутися в головне меню', async (ctx) => {
    ctx.session.mode = null;
    ctx.session.palmReadingType = null;
    await ctx.reply('Головне меню:', Markup.keyboard([
      ['🎴 ТАРО', '✋ Гадання по руці'],
      ['✨ Астрологія', '🔢 Нумерологія'],
      ['💰 Баланс', '🛒 Магазин']
    ]).resize());
  });

  // Встановлення типу аналізу руки
  bot.hears(['👋 Загальний аналіз руки', '❤️ Лінії кохання', '💼 Кар\'єра і достаток', '🧬 Здоров\'я і довголіття'], async (ctx) => {
    if (ctx.session.mode !== 'palmreading') return;
    
    const type = ctx.message.text;
    ctx.session.palmReadingType = type;
    
    const COST = 3; // Вартість аналізу руки - 3 монети
    
    if (!deductCoins(ctx, COST)) {
      await ctx.reply(`Недостатньо монет для аналізу руки. Вартість: ${COST} монет. Поповніть баланс у магазині.`);
      return;
    }
    
    await ctx.reply(`Вибрано режим: ${type} (витрачено ${COST} монет).
    
Будь ласка, завантажте чітке фото своєї долоні. Рекомендації:
- Сфотографуйте внутрішню сторону лівої руки (для правші) або правої руки (для лівші)
- Забезпечте хороше освітлення без тіней
- Розпряміть долоню повністю для кращої видимості ліній
- Фото має бути чітким, щоб було видно всі лінії руки`);
    
    logActivity(ctx, `Вибрав тип аналізу руки: ${type}`);
  });

  // Обробка завантажених фото для аналізу руки
  bot.on('photo', async (ctx) => {
    if (ctx.session.mode !== 'palmreading' || !ctx.session.palmReadingType) return;
    
    try {
      await ctx.reply('Отримую фото вашої долоні. Починаю аналіз...');
      
      // Отримання найбільшої версії фото
      const fileId = ctx.message.photo[ctx.message.photo.length - 1].file_id;
      const fileLink = await ctx.telegram.getFileLink(fileId);
      
      // Тип аналізу
      const analysisType = ctx.session.palmReadingType;
      
      // Запит до OpenAI відповідно до типу аналізу
      let prompt;
      
      switch (analysisType) {
        case '👋 Загальний аналіз руки':
          prompt = `Ти - експерт з хіромантії. Зроби загальний детальний аналіз долоні за фото. 
          Розглянь основні лінії (життя, серця, голови, долі), пагорби, форму руки та пальців.
          Дай загальну характеристику особистості, характеру, особливостей життєвого шляху.
          Висловлюйся як досвідчений хіромант, але зрозуміло для звичайної людини.
          Пиши українською мовою.`;
          break;
          
        case '❤️ Лінії кохання':
          prompt = `Ти - експерт з хіромантії. Зроби детальний аналіз ліній, що стосуються кохання та відносин на долоні:
          лінія серця, шлюбні лінії, лінія Венери, пагорб Венери тощо.
          Опиши емоційну природу людини, її схильності у відносинах, потенційні шлюби/значні стосунки.
          Дай прогноз щодо любовних перспектив.
          Пиши українською мовою, детально, але доступно.`;
          break;
          
        case '💼 Кар\'єра і достаток':
          prompt = `Ти - експерт з хіромантії. Зроби детальний аналіз ліній і знаків, що стосуються кар'єри та фінансів:
          лінія долі, лінія Сонця (успіху), пагорб Юпітера (амбіції), пагорб Меркурія (бізнес), лінія Сатурна.
          Опиши професійні схильності, таланти, потенціал для багатства, можливі професійні досягнення.
          Дай рекомендації щодо найбільш сприятливих сфер діяльності.
          Пиши українською мовою, структуровано і зрозуміло.`;
          break;
          
        case '🧬 Здоров\'я і довголіття':
          prompt = `Ти - експерт з хіромантії. Зроби детальний аналіз ліній і знаків, що стосуються здоров'я та довголіття:
          лінія життя, лінія здоров'я, острови/перерви в лініях.
          Зверни увагу на потенційні схильності до певних проблем зі здоров'ям.
          Опиши загальний енергетичний стан, витривалість, потенційні сильні та слабкі сторони організму.
          Дай рекомендації щодо підтримки здоров'я на основі побаченого в долоні.
          Пиши українською мовою, інформативно, але делікатно.`;
          break;
      }
      
      // Тут ми використовуємо mock-аналіз, оскільки GPT не може аналізувати зображення в реальному часі
      // в повноцінній реалізації це мав би бути запит до GPT-4 Vision або іншої системи аналізу зображень
      await ctx.reply('Аналізую лінії та знаки на вашій долоні...');
      
      const analysis = await callOpenAI(prompt);
      
      await ctx.reply(analysis);
      
      // Очищення сесії
      ctx.session.palmReadingType = null;
      
      logActivity(ctx, `Отримав аналіз руки: ${analysisType}`);
    } catch (error) {
      console.error('Помилка при обробці аналізу руки:', error);
      await ctx.reply('Виникла помилка при аналізі фото. Спробуйте пізніше або завантажте якісніше фото.');
      
      // Повертаємо монети у випадку помилки (3 монети)
      ctx.session.user.coins += 3;
    }
  });
}

module.exports = {
  setupPalmReadingMode
};
